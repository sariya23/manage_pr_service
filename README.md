# Сервис назначения ревьюеров для Pull Request’ов
![Go](https://img.shields.io/badge/Go-1.25-blue?logo=go)
![Docker](https://img.shields.io/badge/Docker-🐳-blue?logo=docker)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-16-blue?logo=postgresql)
## Описание проекта

Сервис для автоматического назначения ревьюеров на 
Pull Request’ы внутри команды. 

Предоставляет REST-API для:
- назначения и переназначения ревьюверов;
- управления командами и пользователями
- получения списка PR’ов;
- назначенных конкретному участнику.

## Файловая структура проекта

```shell
.
├── api
│   └── openapi.yaml       # Спецификация API
├── cmd
│   └── server             # Файлы для запуска сервера
├── config                 # Файлы конфигурации
│   ├── dev.env
│   └── local.env
├── deployments            # Докер файлы для разных окружений
│   └── docker
├── go.mod
├── go.sum
├── internal
│   ├── app                # Сборка компонентов приложения перед запуском
│   ├── config             # Загрузка конфига
│   ├── converters         # Конвертеры моделей
│   ├── generated          # Автосгенерированные модели REST API
│   ├── handlers           # HTTP-ручки, слой презентации
│   ├── lib                # Вспомогательные пакеты
│   ├── models             # Доменные модели и DTO
│   ├── outerror           # Бизнес-ошибки
│   ├── service            # Слой бизнес-логики
│   ├── storage            # Слой данных
│   └── validators         # Валидаторы запросов
├── Makefile
├── migrations             # Миграции
└── README.md
```

## Конфигурация сервиса

Конфигурация происходит через `.env` файлы. Для создания собственной
конфигурации нужно создать файл с по шаблону `<environment>.env` в `./config/`.
Пример наполнения можно посмотреть в файлах `./config`

## Сборка проекта

### LOCAL окружение

В окружении LOCAL все компоненты запускаются в docker-контейнерах.
Окружение применяется для демонстрации работы сервиса на других компьютерах

- `make service_up` - поднять сервис
- `make service_down` - "убить" сервис

По умолчанию сервис работает на порте `8080`

### DEV окружение

В окружении DEV в docker-контейнерах запускается только инфа (БД, Kafka, redis и тд).
Применяется для локальной разработки/дебага

- `make run` - запустить севрер 
- `make infra` - поднять инфру
- `make migrate` - накатить миграции
- `make test` - запуск юнит-тестов

## Вопросы/проблемы

### team/add

Не совсем из описания понятно, что ручка должна делать. 
Я решил сделать у нее такой функционал:
- Создать пустую команду
- Создать команду **только** с участниками, которых до этого в этой команда не было
  - Это либо юзеры без команды
  - Либо новые юзеры

Почему я сделал так: чтобы ограничить возможности ручки и не делать 
из нее god-object. 


